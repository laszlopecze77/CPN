% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cpn_data.R
\name{simulate_cpn_data}
\alias{simulate_cpn_data}
\title{Simulate Data from a Compound Poisson-Normal Model}
\usage{
simulate_cpn_data(n = 100, beta = c(0.5, -0.3, 0.7), mu = 1, sigma = 2)
}
\arguments{
\item{n}{Integer. Number of observations to simulate. Default is 100.}

\item{beta}{Numeric vector. Coefficients for the linear predictor,
including intercept. Default is \code{c(0.5, -0.3, 0.7)}. Must
match the number of columns in the model matrix: intercept, x1B, x2.}

\item{mu}{Numeric. Mean of the Normal distribution for each event.
Default is 1.}

\item{sigma}{Numeric. Standard deviation of the Normal distribution
for each event. Default is 2.}
}
\value{
A \code{data.frame} with \code{n} rows and 3 columns:
\describe{
\item{y}{Numeric response variable generated from the CPN model.}
\item{x1}{Categorical predictor with levels "A" and "B".}
\item{x2}{Continuous predictor drawn from a standard normal
distribution.}
}
}
\description{
This function generates synthetic data based on a Compound
Poisson-Normal (CPN) model. The number of events for each
observation is drawn from a Poisson distribution, and the
outcome is the sum of normally distributed values for each event.
}
\details{
The predictors include a binary categorical variable (\code{x1}) and
a continuous variable (\code{x2}). A linear model with coefficients
\code{beta} is used to model the log of the Poisson rate.
}
\examples{
set.seed(42)
simulated_data <- simulate_cpn_data(n = 200)
head(simulated_data)

}
