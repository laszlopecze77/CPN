#' Compute Bayesian Information Criterion (BIC) for a CPN Model
#'
#' Calculates the BIC for a fitted Compound Poisson-Normal (CPN) model.
#'
#' @param object An object of class \code{"cpn"}, typically produced by a CPN model fitting function.
#' @param ... Additional arguments (currently unused).
#'
#' @return A numeric value representing the BIC of the model.
#'
#' @export
BIC.cpn <- function(object, ...) {
  log(nrow(object$data)) * length(object$se) + 2 * object$neg_log_likelihood
}
